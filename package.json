{
  "name": "ratemates",
  "version": "1.0.0",
  "private": true,
  "description": "RateMates — private Bewertungs-App für Freundesgruppen. Die App selbst ist buildfrei; npm dient nur der Syntaxprüfung.",
  "scripts": {
    "check": "node scripts/check.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@babel/standalone": "^7.23.10"
  }
}
